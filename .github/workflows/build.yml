name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # ここでマウスブランチを直接指定します（west.ymlの設定を上書きして確実に適用させます）
      zmk_branch: "feat/pointers-move-scroll"
      zmk_repo: "petejohanson/zmk"
      # configフォルダの場所を指定
      config_path: "config"
