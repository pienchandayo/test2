#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/mouse.h> // マウス用の定義を追加

/ {
    keymap {
        compatible = "zmk,keymap";
        default_layer {
            bindings = <
                &mkp Z
            >;
        };
    };
};